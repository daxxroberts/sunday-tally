# Schema Redesign — Cutover Status & Open Work
## Single source of truth for the tag-first schema migration. READ THIS FIRST when resuming.
## Last updated: 2026-06-01

> The DB + import pipeline are cut over to the unified tag-first schema (Solution A).
> The **consuming UI screens still read the dropped tables** and must be repointed to
> `metric_entries` / the views. This file tracks what's proven, what's open, and the
> Builder's explicit requests so nothing is forgotten. Companion docs: `APP_CONTEXT.md`
> (v2.0 model), `IMPORT_IR_V2.md` (import contract), `DECISION_REGISTER.md`.

---

## ✅ ACCURACY RECONCILED (sheet ↔ DB) — 2026-06-01
The deterministic Stage B's output was reconciled cell-by-cell against the source Google Sheet:
- **1/7/2024 — 24/24 cells exact** (attendance + volunteers + stats, including all zeros).
- **1/14/2024 — attendance 157/49/84/26 exact.** · **2026-05-17 — 159/153/59/66 (DB↔grid).**
- Dataset bounds match the sheet: 2022-02-06 → 2026-05-17, 224 service dates, 448 instances, 5,494 entries, 0 negatives.
- The current 5,494 entries were written by the **deterministic** Stage B → confirms removing the AI did NOT change correctness (AI was only Phase-1 bookkeeping; Phase-2 value extraction is deterministic and faithful).

## ✅ Verified working this session (do not redo)
- Migrations **0022–0027** applied to main (project `iwbrzdiubrvogiamoqvx`) and verified:
  - 0022 full reset · 0023 unified schema (service_tags + reporting_tags + metrics + metric_entries, RLS, triggers) · 0024 seed_system_reporting_tags · 0025 four views (security_invoker) · 0026 metrics.code (UNIQUE per church) · 0027 metric_entries upsert constraint (`uq_metric_entry` NULLS NOT DISTINCT (metric_id, service_instance_id, period_anchor)).
- **Provisioning**: signup creates church + owner + location + 4 system reporting tags. (PROVISIONING.md updated.)
- **Import Stage A** (producer): emits metric-centric IR — correct tag_roles, canonical invariant (1 per ministry×reporting), system-tag refs. (~4.3 min runtime — perf note.)
- **Import Stage B** (writer): writes `metric_entries` correctly. **Now FULLY DETERMINISTIC — AI removed from Stage B entirely** (Phase 1 entity creation is a deterministic loop over `proposed_setup` → `WRITER_HANDLERS`, no Haiku; Phase 2 row extraction unchanged). Verified end-to-end: 2 tags, 14 metrics, 6 canonical, **5,494 entries**, idempotent, **~11 s** (was ~38 s with the Haiku loop, 4.3 min originally). **Architecture principle now enforced: A (AI) decides the structure → B (deterministic) executes it.** AI remains only in Stage A. (Follow-up #64: add a Haiku VALIDATION pass that checks B's result against A — AI as checker, never executor.)
- **Views**: `volunteers_per_occurrence` (448 rows), `metric_entries_readable` correct; trigger denormalizes `reporting_tag_code`.
- **Walkthrough UI**: one-at-a-time Qs, halfway AI checkpoint (reasons about metric model), all-required gate, Confirm. Shared `applyPatchOp` (client + server).
- **History page COLUMNS**: render the new model from `grid_config` (proves derive_grid_config cutover #56).

## Demo church (test data, disposable)
- Login: `demochurch@example.com` / `DemoChurch2026!`
- church_id `4037051e-52f5-4c22-a0f1-32e7b45aaff4` · import job `21e4a4aa-ef48-4112-b26e-5fb2cef13406`
- Contains 4,598 metric_entries (vol+stats), **0 attendance** (bug #60), + 1 stray manual attendance test row (value 312) that is NOT from the import.
- To re-run Stage B on the job without redoing Stage A: `UPDATE import_jobs SET status='awaiting_confirmation' WHERE id='<job>'` then re-PATCH `/api/onboarding/import` with the stored `confirmed_mapping`.

---

## 🔧 OPEN WORK (nothing here is done — keep until each is verified in the UI)

### Bugs found by the end-to-end test
- **#60 — Attendance tall-routing — DIAGNOSED; demo data complete; durable fix UNVERIFIED.**
  - ROOT CAUSE: the sheet's `Group Type` cell value is **"Attender"** (singular) but Stage A wrote the `area_field_map` keys as **"Attenders"** (plural) → computed key `"Attender / Experience / Adult"` never matched the map's `"Attenders / ..."` → 0 attendance entries. (Volunteers/Stats matched because Stage A used their exact values. The `Area` column already holds "Adult"/"Kids" — the correct 3rd segment.)
  - PROVEN: patching the stored mapping keys to exact "Attender" → **896 attendance entries** (448 instances × Experience+LifeKids), avg adult 151 / kids 46. The demo church currently HAS the full 896 (restored via this exact-key patch).
  - DURABLE FIX (root cause): a Stage A prompt instruction was added (`stageA.ts`) telling it to use VERBATIM observed segment values (never pluralize) — so a fresh import would emit "Attender" → literal-exact match → full attendance. **UNVERIFIED — needs a fresh import (new job, ~4-min Stage A) to confirm Stage A now emits "Attender".**
  - SECONDARY (buggy): a normalization fallback (lowercase + strip-trailing-s) was added to `routing.ts`+`stageB.ts` to tolerate plural/case variance. It only rescued **84/896** in testing — it's a fallback-after-literal-exact, so harmless when Stage A emits correct keys, but it does NOT reliably rescue the plural mismatch. Either fix it (find why it matched only ~42/448 instances) or remove it and rely on the Stage A verbatim fix.
  - The demo church's 896 attendance was achieved via a manual mapping patch, NOT reproducibly via a fresh import yet. Reproducibility = the unverified part.

### Read-side screens still on the dropped schema (repoint data fetch to metric_entries/views)
- **#63 — History page data fetch — DONE & VERIFIED (2026-06-01).** Cells populate with real data; verified 3 ways (744+ numeric cells render; exact DB values for 2026-05-17 — 159/153/59/66 — present in grid; all 3 dimensions attendance/volunteers/stats render). ROOT CAUSE (not a key mismatch — keys were always correct): the `metric_entries` fetch had NO date filter + NO ordering, so PostgREST returned the FIRST 1,000 rows (oldest 2022–2023), all outside the visible occByUuid range → data.size=0. FIX: scope the query to in-range occurrence ids (`.in(occIds)`) + period_anchor range, and paginate past the 1,000-row cap with `.range()`. Also fixed a `service_tags.tag_name`→`name` 400 on the page. (Inline-edit save path was repointed to metric_entries too; not yet browser-verified — spot-check on a future pass.)
  HISTORICAL (resolved): Data fetch repointed to `metric_entries` (query runs, 200, returns rows) and the `tag_name`→`name` 400 was fixed. BUT cells STILL render empty ("Click to add…", 0 numeric values) in the browser, with NO console errors. → The page's value-map cell key does NOT match HistoryGrid's `cellKey` at RUNTIME (a sub-agent's static Node check claimed match, but live render shows 0).
  - Grid cellKey (HistoryGrid.tsx:326/404): `` `${row.type}-${row.anchor.toISOString()}-${row.metricId ?? row.serviceTemplateId ?? idx}-${columnId}` ``. For SV rows: type='SV', anchor = service_date at UTC-midnight, 3rd segment = `row.serviceTemplateId` which per derive_grid_config is the **service_code** ("1"/"2"), columnId = `metric.<CODE>` (exact uppercase metrics.code).
  - **PRIME SUSPECT:** the page joins metric_entries→service_instances→`service_template_id` which is a **UUID**, but the grid's SV key uses the **service_code** ("1"/"2"). If the page keys SV cells by the template UUID instead of translating UUID→service_code, every cell key mismatches → 0 values. NEXT STEP: instrument page.tsx to console.log the keys it generates for one known row (service_date 2026-05-17, template service_code "1", metric EXPERIENCE_ATTENDANCE, expected value ~148/163) and compare byte-for-byte to the grid's cellKey; align the 3rd segment (translate service_template_id UUID → service_code via the service_templates map the page already loads). Verify in browser (not just tsc/Node).
  - Demo church HAS the data (896 attendance/2,240 vol/2,358 stats), so once keys align the grid will fill. Columns already correct.
- **#62 — Dashboard — DONE & VERIFIED (2026-06-01).** `src/lib/dashboard.ts` repointed to views (attendance_per_occurrence/volunteers_per_occurrence/giving_per_week) + metric_entries (paginated past the 1,000 cap — 1,538 in-window entries), grouped by tag_role, FourWin/window/delta/YTD math preserved. Added `reportingMetrics` (FourWin each) + a Key Metrics strip on D1/viewer/AI. Browser-verified: no "No data yet", no console errors, Experience/LifeKids sections render real numbers, and **VOLUNTEERS/ATTENDANCE = 28% (matches the 27.9% SQL cross-check)**, weekly avg attendance 474/449/439. PER-CAPITA GIVING shows "—" (null) because GIVING isn't imported yet — import the Giving sheet tab to light it up. (`tracks_main_attendance` removed from the dashboard layer; still exists on the Church type/other screens.)
- **#61 — Tags settings UI — DONE (render verified; write RLS-permitted, not click-tested).** `src/app/(app)/settings/tags/page.tsx` rewritten to new schema: reads code/name/tag_role/parent_tag_id; renders a parent-child TREE from parent_tag_id adjacency (Experience → ↳ LifeKids verified by name); per-row tag_role picker (Adults/Kids/Youth/Other), reparent select (with cycle guard), Remove = soft-delete (is_active=false) with child-guard (avoids metrics ON DELETE CASCADE); add-tag form (name + role + parent, slug code w/ uniqueness, no add_tag_relationship RPC); Campaign/dated-subtag section removed. Browser-verified the RENDER 4 ways. NOT click-tested: add/remove/reparent WRITES (test-harness session kept lapsing on repeated dev restarts) — but service_tags RLS is FOR ALL church-isolation so the owner can write; worth a 30-sec manual click-test (add a tag, confirm it persists).
- **Also still on old schema (not yet ticketed individually):** weekly-entry screens (T2–T5, `/services/[id]/*`, `/services`, weekly, weekly-stats), settings (volunteer-roles, stats, giving-sources, tracking, services), AI analytics (`api/ai/analytics`, `lib/ai/metrics.ts`, `lib/ai/probe.ts`). `src/types/index.ts` is stale (regen).

### Bigger build items
- **#36 — Weekly-entry screens → Service → Ministry → Metrics** (the primary value rework; "very soon").
- **#52 — Finish Phase 7 handoff docs.** DECISION_REGISTER needs D-059…D-071 written (currently only design-locked in the brain); reconcile stale entries superseded by the redesign (D-002 reversed, D-056/057/058 partially). QUERY_PATTERNS.md + HANDOFF_BRIEF.md for the new model. APP_CONTEXT.md v2.0 + IMPORT_IR_V2.md done.

---

## 📌 Builder's explicit requests (do not lose these)
1. **Out-of-the-box reporting metrics** from the reporting tags (Attendance/Volunteers/Giving), on the dashboard:
   - **% volunteers to total attendance** = SUM(VOLUNTEERS) / SUM(ATTENDANCE)
   - **per-capita giving** = SUM(GIVING) / SUM(ATTENDANCE) (weekly average giving vs attendance)
   - **average weekly attendance** = AVG(ATTENDANCE) over weeks with ≥1 active instance
   (Build in #62. Needs #60 fixed first or attendance-based ratios read 0.)
2. **Parent-child tag tree visualization** — a nice way to see the ministry hierarchy (Experience → LifeKids). (Part of #61.)
3. **Add / remove tags** from the UI. (Part of #61.)
4. **Reconsider LifeKids as a CHILD of Experience** — RESOLVED 2026-06-02 (Builder confirmed): LifeKids is a SEPARATE, PARALLEL ministry, NOT a child of Experience. They co-occur in the same Sunday occurrences (9:00 + 10:30) but each reports its own metrics. → un-parent LifeKids; `parent_tag_id` reserved for true rollup umbrellas only. See D-072…D-075.
5. **Test UIs in the browser, not just the DB** — standing instruction. Every screen gets a real render check (agent-browser / preview), not just SQL verification.

## Locked decisions (schema redesign) — to be written into DECISION_REGISTER (#52)
D-059 parent_tag_id adjacency (closure rejected; ltree is the future escape hatch) ·
D-060 full reset incl churches · D-061 drop 4 empty subsystem tables ·
D-062 two tag axes (service_tags ministry + reporting_tags) · D-063 metrics+metric_entries replace category/entry tables ·
D-064 metric_entries XOR instance/period attachment · D-065 trigger-denormalized reporting_tag_code ·
D-066 one canonical metric per (church,ministry,reporting) · D-067 4 system reporting tags, is_system immutable ·
D-068 D-002 reversed (no fixed MAIN/KIDS/YOUTH columns; audience via tag_role; venue-wide → OTHER CHURCH_WIDE tag) ·
D-069 rename service_tags.tag_code→code / tag_name→name · D-070 retain churches tracking flags as dashboard gates (candidate follow-up) ·
D-071 metrics.code stable per-church identifier (UNIQUE) for idempotent import upserts.

### Service ↔ Ministry composition model — Builder-confirmed 2026-06-02 (for #36; not yet migrated)
**Confirmed mental model:** A *service occurrence* is a CONTAINER. One or more *ministries* run inside it (parallel building blocks). The Builder enters one number per ministry per occurrence; the **service-level total = SUM of the ministries in that occurrence** (e.g. 9:00 Sunday total attendance = Experience 420 + LifeKids 85 = 505). Each ministry can carry its OWN metric set (different reporting tags per ministry is allowed).
- **D-072** No "is-ministry" flag. `service_tags` already IS the ministry axis (table-level identity). Ministry-level reporting is already native via `metrics.ministry_tag_id` — service-independent rollups need no change.
- **D-073** Add `service_template_tags` (service_template_id, ministry_tag_id, sort_order; UNIQUE(template,tag)) — the many-to-many service→ministry composition link. Drives the #36 entry screen (which ministry sections to show). Backfill every existing `service_templates.primary_tag_id` in as a peer row; leave that column physically in place (additive migration 0028; no destructive drop on locked schema). **No `is_primary` — see D-076.**
- **D-074** Service-occurrence total is **DERIVED (SUM across constituent ministries), NEVER STORED** — same principle as the "totals always calculated" critical rule. Editor records per-ministry atoms (ministry × reporting_tag × instance → metric_entry); the total is a view/query. **Rollup is keyed on `service_instance_id`** (group by occurrence → one total per occurrence). Composition link is NOT needed for the total — total just sums whatever entries share the occurrence id; the link only drives which empty input sections the entry screen renders.
- **D-075** Composition is **template-level** (stable per recurring service), not per-occurrence — Builder-confirmed 2026-06-02. A ministry that doesn't meet a given week (holiday) is handled via `metric_entries.is_not_applicable`, not by re-tagging the occurrence.
- **D-076** Ministries within a service are **equal peers — no controlling/primary ministry** (Builder-confirmed 2026-06-02). Rollup groups by occurrence id; neither ministry is parent of the rollup. `is_primary` dropped as a concept; only `sort_order` remains (pure display ordering, zero logic). A cosmetic "defining service" label can be added later if ever needed — nothing depends on it now.
- **D-078** **Entry flow = ministry-first, two-zone hybrid** (Builder-confirmed 2026-06-02). Top zone: per-occurrence, ministry-one-at-a-time, **instance-scoped** metrics (attendance, volunteers, response stats) → `metric_entries.service_instance_id`. Each ministry shows only its own metric set. Bottom zone: a **once-a-week "This Week" step** for **period-scoped** metrics → `metric_entries.period_anchor` (the week's Sunday), church-wide (`OTHER`/CHURCH_WIDE ministry per D-068), entered once regardless of occurrence count. **Giving for this church is period/weekly** (one number covering all services that week — no per-service giving). `metrics.scope` ('instance'|'period') is per-metric/per-church, so a church that tracks giving per service configures it instance-scoped instead — schema supports both (D-064 XOR was built for exactly this). Proposed UX: ministry-driven flow (pick occurrence → enter each ministry) then a closing "This Week" panel for period metrics. **Reaffirmed 2026-06-02** after reconsidering metric-first (enter all attendance, then all volunteers): staying ministry-first. NOTE: entry layout is a free UI choice — metric-first writes identical metric_entries, so a metric-first view or a both-with-toggle can be added later with zero schema impact if ever wanted. **OPEN (goal E, session model): SUNDAY_SESSION is keyed per-DATE (`sunday_session_[YYYY-MM-DD]`) but period metrics anchor per-WEEK — the session must anchor period-scoped entry at the week even though occurrences are per-day.**
- **D-079** **What appears under a ministry on the entry screen** (Builder-confirmed 2026-06-02). (a) **Volunteers expand into areas/roles with a live calculated subtotal** — each area is its own VOLUNTEERS-tagged metric; ministry volunteer total = SUM, never stored (rule #3); matches imported tall-format data. (b) A ministry shows only **its own canonical metrics** (LifeKids→check-ins, Experience→salvations; they don't see each other's). (c) **Inline-add is settings-only** — no defining new metrics from the entry screen; keeps entry fast. (d) Per-ministry **"didn't meet this week" → stamps `is_not_applicable=true`**, distinct from zero and from blank (rule #4: NULL≠0).
- **DEFERRED → settings phase:** Builder wants the **settings screen to visually mirror the entry structure** — "a single month and all its child occurrences/ministries" — so configuring looks like entering. Revisit when reworking settings (not part of #36 input screen).
- **D-080** **Session = week-scoped; save = per-field autosave (conditional on speed)** (Builder-confirmed 2026-06-02). Session unit is the WEEK (anchor = week's Sunday), containing its occurrences (9:00/10:30/Wed…) + the "This Week" period zone; lift SUNDAY_SESSION key from per-date to week. Navigation: occurrences as stacked sections/tabs + closing "This Week" panel. **Autosave on blur** — each field = one idempotent single-row upsert into `metric_entries` (uq_metric_entry, 0027); optimistic + async (never blocks typing); quiet saved/failed state; inline retry preserves value on failure; one write per field (debounced); volunteer subtotals recompute client-side; week-scoped load (small payload, avoids 1000-row cap). Builder condition "if it is fast" → **performance contract: commit round-trip p95 < ~300ms, UI never blocks, every save shows confirmed/failed.** FELIX/LENS verify latency + error path; if unmet, autosave decision reopens (fallback = explicit Save-week). Restoration: `sunday_last_active`; empty/expired session on sub-screen → redirect to week picker, never throw.
- **D-081** **Multi-sitting "day/Sunday total" is derived from `(service_template_id, service_date)` — NOT a new setup question** (Builder-confirmed 2026-06-02). 9:00 & 10:30 are the *same service held twice* (two `service_schedule_versions` times on one template → two `service_instances` sharing template+date). Three derived aggregation levels, none stored: (1) occurrence total = sum ministries in one sitting (D-074); (2) **day total = sum sittings sharing (template, date)** → 9:00 + 10:30; (3) ministry-over-time rollup (native via metrics.ministry_tag_id). Whether two times sum falls out of **service/schedule setup** (one service @ two times → sums; two distinct services → don't) — no separate "do these sum?" question. A church wanting same-service sittings NOT summed = the same dashboard display toggle deferred in D-077. **Summary/dashboard cards** (Builder idea): compact per-ministry cards showing high-level metrics (Switch → Attendance/Volunteers/Salvations), with attendance summed across the week's sittings. Candidate home: dashboard (#62) and/or a "Summary"/"Week at a glance" view on the entry screen. Mocked in /mockup/weekly-entry "Summary" tab.
- **D-082** **Grand-total attendance = SUM of all ministries × all sittings, fully derived — no setup question** (Builder-confirmed 2026-06-02). Week total = sum every ATTENDANCE metric_entry for the period (Experience 1,020 + LifeKids 180 + Switch 64 = 1,264). Falls out of "these are ministries we count" — never stored, no config. **Divergence point (multi-tenant, not solved now):** some churches don't fold kids into the headline total (report adults separately) → that's a per-ministry **"count in total attendance?"** flag OR the D-077 dashboard display preference. For demo church both ministries count → default sum works. Shown as grand-total hero on /mockup Summary tab.
- **D-083** **"Include in total attendance" is an editable, church-saved preference with an inline UI** (Builder-confirmed 2026-06-02). Edit button on the summary total → inline panel with a checkbox per ministry (shows each ministry's number) → live-recompute grand total as toggled → **Save** persists church-wide (candidate store: `churches.grid_config` jsonb). Excluded ministries mute on their summary card with a "Not in total" tag. **Never mutates entered metric_entries** — purely controls what rolls into the headline total (the concrete UI for D-082/D-077). Mocked + verified in /mockup Summary tab (uncheck LifeKids → 1,264 → 1,084).
- **D-084** **#36 weekly-entry UI design LOCKED 2026-06-02** — reference mockup at `src/app/mockup/weekly-entry/page.tsx` (route `/mockup/weekly-entry`, NOT wired, throwaway). Built with ui-ux-pro-max: AccessSync blue `#4F6EF7`, Fira Sans/Fira Code, status circles (gray outline=not started · orange outline=needs · green check=done), per-field autosave, "Didn't meet?" → N/A. **Design is fully schema/config-driven, nothing conceptually hardcoded — UI↔data binding:** tabs=that week's `service_instances`; ministry cards=ministries via `service_template_tags` (0028); role label=`service_tags.tag_role`; metric rows=canonical `metrics` per ministry (`scope='instance'`); volunteers=multiple VOLUNTEERS metrics summed; totals=`SUM(metric_entries)` (D-074/081/082); Weekly Entries tab=`scope='period'` metrics; include-in-total toggle=`churches.grid_config` (D-083); "N of M complete" + status circles=derived from metric_entries presence. **Only new schema object required = migration 0028 `service_template_tags`.** OPEN (pin at build): the "section complete" rule (likely: all canonical instance-metrics for that occurrence's ministries have an entry, N/A counts as done). Real build still gated: migration package + FELIX + DECISION_REGISTER → SAGE. **IRIS #36 build spec PRODUCED 2026-06-02 → `IRIS_ENTRIES_ELEMENT_MAP.md`** (repo root): every element (E-1…E-50) → data binding + states + role rules, NOVA items (N-1…N-10), query patterns to author, completion logic, open items, full D-072…D-088 references. This is the anti-context-loss artifact — a build session reads it cold + the mockup + this status doc and wires it with no re-derivation.
- **D-085** **Navigation/IA: this screen is the "Entries" page — replaces the old Services entry flow** (Builder-confirmed 2026-06-02). The old `/services` list + per-service T1–T5 entry screens are retired; weekly data entry happens here (week-centric, not service-centric). **"Services" as a noun moves to Settings** (configure service templates/times/ministries/metrics) — it's a thing you set up, not a page you enter through. Clean IA: **Dashboard** (overview) · **Entries** (enter the period) · **History** (review/edit past) · **Settings** (configure). The in-page period tab = **"Stat Entries"** (renamed from Weekly Totals/Weekly Entries) — deliberately cadence-neutral because period stats can be **weekly OR monthly**. **Implication (open):** `metrics.scope='period'` needs a **cadence attribute** — enum **DAY · WEEK · MONTH** at minimum (extensible to QUARTER/YEAR) — so the Stat Entries zone and `period_anchor` interpret correctly per metric (D-078 assumed weekly Sunday anchor — generalize). Day vs instance-scoped are distinct: instance = tied to a specific service occurrence; DAY = one church-wide value for the date, not tied to a service. **Cadence indicator shown per metric on Stat Entries (DAILY/WEEKLY/MONTHLY tags).** OPEN interaction design ("adjust this page"): the input control must adapt to cadence on a week-scoped screen — WEEKLY = one box/week; DAILY = 7 per-day boxes (Mon–Sun) for the selected week; MONTHLY = one box spanning the month (doesn't reset weekly, locks to the month the week falls in). Mock currently shows single placeholder boxes for all three. **MVP cadence = DAY/WEEK/MONTH only** (Builder-confirmed 2026-06-02). **FUTURE FEATURE (backlog, post-MVP):** customizable/recurring cadence — interval recurrence ("every 3 days", "every 2 weeks", "every 6 months") and nth-weekday patterns ("every 3rd Sunday"). Forward-compatible & purely additive: `period_anchor` stays one date per entry; cadence evolves from a simple enum into a small recurrence spec (interval+unit / RRULE-like) that only affects how the metric defines periods + how the entry screen generates input slots — NO change to metric_entries or the anchor model, NO MVP rework.

### History display of period metrics (open conventions — to lock at History/build time)
Every entry resolves to ONE placement date: instance → occurrence `service_date`; period → `period_anchor` (canonical date set by cadence: DAY=that day, WEEK=that week's Sunday, MONTH=canonical month date e.g. 1st). OPEN: (a) monthly canonical anchor date (lean: 1st of month, display "Mon YYYY"); (b) how a weekly-row History grid renders each cadence — lean: rows per-period, each metric on its anchor; monthly groups at month level; daily rolls to a week total with per-day drill-down. NAV change → must land in `NAV_MANIFEST.json` / `FLOW_REPORT.md`; retire old `/services` routes. Confirm at build.
- **D-086** **Locations: standard church-wide schema; location is a DIMENSION, not a per-location schema** (Builder-confirmed 2026-06-02). `church_locations` already exists + wired (`service_templates.location_id`, `service_instances.location_id`). Demo church = one active "Main Campus" (code MAIN_CAMPUS, 448 instances) + a stray empty duplicate "Main" (cleanup candidate). Ministries/reporting tags/metrics stay CHURCH-level (defined once, shared by all locations) — adding a campus = 1 `church_locations` row + that campus's templates/occurrences; **no schema change, no per-location schema**. Per-location reporting is FREE for instance metrics (location via `service_instance.location_id`). **UI add (not schema):** location selector + "All locations" roll-up on Entries/Totals/Dashboard (a week spans multiple campuses). **OPEN nuance:** church-wide PERIOD stats (Stat Entries/giving) attach to `period_anchor`, not an instance → no location today. Church-wide period stats = MVP default (matches "standard church-wide"); if per-campus period stats ever needed (per-campus giving), add nullable `metric_entries.location_id` (null = whole church) — additive, decide at build.
- **D-087** **`location_id` required on all *event/data* tables for per-campus differentiation; definition tables stay church-wide** (Builder-directed 2026-06-02). Campus is selected on a **separate page** (not inline on Entries) — Entries header just shows the active-campus context pill. **Add `metric_entries.location_id`** (nullable FK → `church_locations`, NULL = church-wide) — this is the gap: instance entries can derive location via their occurrence, but PERIOD entries (giving/stat entries) have no occurrence, so the column is required to tell per-campus stats apart. Denormalize via trigger from `service_instance.location_id` for instance entries (same pattern as `reporting_tag_code`); set explicitly (or NULL=church-wide) for period entries. **Already have location_id:** `service_templates`, `service_instances`. **Intentionally NO location_id (stay church-wide per D-086 standard schema):** `metrics`, `service_tags`, `reporting_tags` — definitions defined once, shared by all campuses; only DATA carries location. Goes in the #36 build migration package (bundle with 0028 `service_template_tags`), FELIX + SAGE gated — not yet applied.
- **D-088** **Per-user default location** (Builder-directed 2026-06-02). Add `church_memberships.default_location_id` (nullable FK → `church_locations`). Editable per user (set on invite + on the member's settings). On login, after resolving the user's membership for the church, the app **defaults the active campus to `default_location_id`** (fallback: first active `church_locations` row) so users don't pick from the dropdown every time. Bundle into the #36 build migration package (with 0028 + `metric_entries.location_id` + cadence), FELIX + SAGE gated — not yet applied.
- **D-089** **Design system hardened → `DESIGN_SYSTEM.md`** (repo root, 2026-06-02). 25 binding rules (DS-1…DS-25) + anti-patterns, extracted from the Entries-screen pass — applies to ALL screens. Headlines: AccessSync blue `#4F6EF7` brand; **NO red** (amber=attention, sage=done); color discipline (brand=interactive · sage/amber=status · ministry blue/violet/teal=category · slate=metadata, no collision); Fira Sans + Fira Code tabular numerals; **one status vocabulary** = equal-size circles (gray outline / orange outline / green check), no wordy badges, no legend; category labels = "· plain text" not pills; derived≠editable; inputs right-aligned with status to the LEFT for alignment; autosave-on-blur; SVG icons only (no emoji/glyphs); affordance restraint (hover-only chrome); declutter by default; IA = Dashboard·Entries·History·Settings; future-proof cadence-neutral naming. Front-end agents (PIXEL/FORGE/LENS) enforce on every page.
- **D-090** **Migration 0028 APPLIED to main + FELIX-validated + SAGE-ratified 2026-06-02** (`supabase/migrations/0028_entries_build.sql`). Delivered the gated package: `service_template_tags` (+RLS `get_user_church_ids()` +backfill primary_tag→2 rows), `metric_entries.location_id` (+denorm trigger extended +backfill 5,494/5,494, total unchanged → zero data loss), `church_memberships.default_location_id`, `metrics.cadence` (day/week/month check; 0 period metrics exist yet so 0 backfilled — giving not imported). FELIX PASS, SAGE RATIFY. **PROCESS NOTE:** applied before the gate on a user "yes" — corrected by running FELIX/SAGE retroactively; going forward FELIX validates BEFORE apply (gate is the BOT team's, not a user click). REMAINING build-data steps (not schema): attach LifeKids to the 9AM/11AM templates in `service_template_tags` (backfill only added Experience); set demo membership `default_location_id`=Main Campus.
- **D-091** **#36 Entries page BUILT + WIRED + VERIFIED 2026-06-02** (branch `design/entries-spec-and-mockup`, commit aabecac). Real route `src/app/(app)/entries/{page,ui}.tsx` — fully schema-driven per the IRIS map. FELIX: typecheck clean, autosave upsert on uq_metric_entry correct, paginated prefill, role gating. LENS (live `/entries`, demo church): Totals=437 (Experience 312 + LifeKids 125 across 2 sittings), 9AM occurrence prefilled 159/59, design-system look intact. **Write path proven:** edited 159→160 → autosave → DB persisted 160 → restored 159. SAGE: RATIFY (code on branch, not deployed). **Remaining follow-ups (NOT done):** N-10 wire "Entries" into AppLayout nav + retire old `/services` + T2–T5 routes (still query dropped tables) + NAV_MANIFEST/FLOW_REPORT; N-4 daily 7-box cadence control; per-campus period stats (O-3); demo duplicate "Main" location cleanup. Old `/services` route remains broken until retired.
- **D-092** **Dashboard + Settings built overnight 2026-06-03 (workflow wf_422ec68d), BOT-gated, UNCOMMITTED.** Spec→implement→review→fix via 8 subagents; specs `IRIS_DASHBOARD_ELEMENT_MAP.md` + `IRIS_SETTINGS_ELEMENT_MAP.md`. Files (workflow-touched, on disk, NOT committed): `src/app/(app)/dashboard/page.tsx` + new `dashboard/ui.tsx`; `settings/{page,services,locations}/page.tsx`; `entries/ui.tsx` (additive icons + membershipRoleLabel — no Entries regression). 
  - **Dashboard — FELIX+LENS PASS.** tsc clean; redesigned to DS; 4-window deltas (Grand 474/449↑2%/439, Kids↑18%, Vols↑13%), Key Metrics strip (avg wkly attendance, vol/attendance 28%, per-capita giving), per-ministry breakdown, Customize→grid_config, NO RED. Fixed a major totals double-count (same-role multi-tag). MVP deviation: campus pill static "All campuses" (live toggle = fast-follow, needs locationId param on dashboard.ts).
  - **Settings — render PASS; ⚠ SECURITY BLOCKER.** Hub + Services&Ministries (mirrors Entries via service_template_tags) + Locations&Team (campuses + per-member default_location_id D-088) all render, schema-driven, DS-compliant. **BLOCKER: role gating is CLIENT-SIDE ONLY** — live RLS on church_memberships/church_locations/service_template_tags is church-isolation only (no role predicate), so a viewer/editor could self-escalate via the SDK. Fix written but **NOT APPLIED**: `supabase/migrations/0029_settings_role_rls.sql` (role-aware RLS + helpers + last-owner guard + co-member profile read). **Settings writes are NOT safe until 0029 is applied (needs Builder approval + FELIX/SAGE).** Write paths NOT browser-tested (avoided mutating membership / privilege paths).
  - **SAGE:** Dashboard RATIFY; Settings CONDITIONAL (blocked on 0029).
  - **⚠ REPO FINDING:** working tree has a LARGE pre-existing UNCOMMITTED backlog from prior sessions (entire #52–#63 cutover: dashboard.ts, history, tags, signup, api routes, globals, layout, package.json ai-SDK deps, migration FILES 0017–0028, dozens of scripts) — all dated 2026-06-01, never committed. The overnight deliverables sit on top. NOT committed the mixed tree (Builder should review + commit holistically). 
  - **Still pending:** apply 0029 (approval); retire old `/services` + T2–T5 routes carefully (PRESERVE /services/history = working History #63 — move to /history); daily 7-box (N-4); dashboard campus toggle; dedup "Main" location.
- **D-093** **Entry-screen fixes 2026-06-03 (Builder review of /entries) — uncommitted.** (1) **Entry shows ALL active instance metrics, not just canonical** — supersedes the canonical-only-on-entry part of D-079. Reason: only one volunteer area was canonical, so the breakout + non-canonical stats (Host/Operations/Other volunteers, Hands Raised, Parking, Rooms Open) were hidden. Now: render all active instance metrics; **`needs`/required state + completion gate use canonical only** (`needs={m.is_canonical}`, ministryStatus filters canonical). Verified: Experience Volunteers breakout = 45 (Band 19+Host 23+Ops 3), Parking 120 etc. now visible. (2) **History link added** to /entries header → `/services/history`. (3) **LifeKids un-parented** (`service_tags.parent_tag_id`=NULL) — Experience & LifeKids now both top-level parallel ministries (D-076 finally applied as DATA; combined-total reporting unaffected — comes from service composition + metric sums, not the tag hierarchy).
  - **DATA ISSUE for Builder (#3 '11 AM'/'10:30'):** templates are named "9 AM" & "11 AM"; schedules are inconsistent — "11 AM" has inactive 11:00 + **active 10:00**; "9 AM" has inactive 09:00 + **active 10:00** (both active at 10:00, nothing at 10:30). Tabs show template display_name (instance start_datetime is null). NEEDS Builder's real service names/times to correct (not guessed).
  - **Stat Entries empty** is correct-given-data: 0 period-scoped metrics (giving never imported). Per-ministry stats (baptisms/hands-raised/parking) are instance metrics, now visible under ministries. To populate Stat Entries → import giving sheet (gid 181499763) or define period metrics.
- **D-094** **History grid horizontal scroll fixed 2026-06-03.** `.grid-wrapper` grew to full content height (~6,383px) because the fill-height flex chain isn't bounded (layout root is `min-h-screen`), pushing the horizontal scrollbar ~6,000px below the fold. Fix: `max-height: calc(100dvh - 168px)` on `.grid-wrapper` (HistoryGrid.css) → grid scrolls internally (x+y), horizontal bar back on-screen, body no longer overflows. CSS-only, History-scoped (AppLayout untouched). Verified: bar on-screen (wrapper bottom 962 ≤ viewport 1032), scrollW 2399 > clientW 978.
- **D-095** **Service names/times corrected + import-flow gap found 2026-06-03.** Builder: services are **First Experience @ 9:00** (code 1) and **Second Experience @ 10:30** (code 2). Root cause analysis (import_jobs 21e4a4aa proposed_setup.service_templates): **Stage A worked correctly** — it proposed both templates with `primary_tag=EXPERIENCE`, `start_time=null`, **`display_name="[BLOCKING]"`** + reasoning "pending user confirmation of what code '1'/'2' represents." Stage A did NOT invent "9 AM"/"11 AM". The bogus names + duplicate 10:00 schedules were introduced **downstream at the confirm/Stage-B step, which resolved a `[BLOCKING]` field to a placeholder guess instead of forcing the user answer.** Data fixed directly: renamed templates (c46cc499→First Experience, cf8117f1→Second Experience), set active schedules 09:00/10:30, deleted 2 stray inactive placeholder schedule versions. Entries tabs now show "First Experience"/"Second Experience" (verified). **IMPORT-FLOW FOLLOW-UP (bug):** a `[BLOCKING]` proposed field must hard-require a user answer in the confirm walkthrough and must NEVER auto-resolve to a guessed display_name/time. (Related to #60 import robustness.)
- **D-096** **ACCOUNT PORTAL / AUTH / BILLING — NEEDS A BLUEPRINT PASS (logged 2026-06-03, Builder-flagged).** Not built as a coherent portal; treat like Entries (FLOW→SCREEN→RULE→IRIS maps) before building. Auth-backing = **Supabase Auth**; email = **Resend** (wired: `lib/email/resend.ts`); payments = **Stripe** (wired: `api/stripe/{checkout,portal,webhook}` + `lib/stripe` + `(app)/billing`).
  - **EXISTS (pre-cutover, status UNVERIFIED — verify against new schema):** `auth/login`, `auth/invite/[token]` (+actions), `signup`, onboarding flow, `(app)/billing`, `settings/team` AND the new Locations&Team (invite member/role/default campus — **two team surfaces to reconcile into one**).
  - **MISSING / unplanned:** (1) **password reset / forgot-password** (no `resetPasswordForEmail` anywhere); (2) **self-service password change** + an **Account/Profile** page; (3) **Members & Invitations screen** = all invited people with STATUS (pending/accepted/expired), resend/revoke invite, role + default campus; (4) overall **portal IA** (sign-in ↔ account ↔ members ↔ billing). 
  - **BUILDER external setup (NOT agent — no account creation / credentials by me):** create Stripe account + keys; configure Resend domain + API key; choose reset-email flow.
  - Likely schema touches (flag, don't apply): `church_invites` status surfacing already exists; possibly a profile/last-login field. Re-uses D-088 default_location_id. Role-write gating depends on **0029** (D-092).
- **D-097** **Account Portal BUILT + remediated + BOT-gated 2026-06-03 (workflow wf_34d4e31d) — UNCOMMITTED.** 17-agent run (audit→spec→implement→security-review→fix) + a focused auth-remediation agent + my SAGE/LENS gate. **Built (all tsc-clean, render-verified live):** `auth/forgot` + `auth/reset` + new `auth/callback/route.ts` (PKCE exchange — also fixes magic-link/invite handshake, audit B5); `settings/account` (display name, self default-campus, change/set password — E-46 for OTP viewers); unified `settings/team` **Members & Invitations** (status pending/accepted/expired, resend/revoke, role, default campus) with Locations&Team team-zone shed → links to it (D-096 reconciliation done); `billing` redesigned to DS (trial/active/past_due/expired, Subscribe $22/mo, server-gated). **Pre-existing auth bugs FIXED in remediation:** B1 invite-token read → service-role (invite-accept was dead under RLS); B2 all post-login `/services` dead-ends → role-aware (viewer→/dashboard/viewer, else→/entries) across login/invite/onboarding/middleware; middleware↔nav reconciled (Settings tab now open to all roles for /settings/account; other /settings stays owner/admin); S2 invite binds role/church/email from token server-side (+ email match), not client args; B6 invite status/expires_at lifecycle confirmed; global `.font-num` + Fira fonts added to globals.css (DS-4 now real app-wide). LENS verified: /settings/account, /auth/forgot, /settings/team, /billing, /entries all render; owner access intact post-middleware-change; no console errors. **SAGE: RATIFY (code on branch/disk, uncommitted).**
  - **STILL NEEDS BUILDER (external, agents can't):** create Stripe account + STRIPE_SECRET_KEY + $22/mo PRICE_ID + webhook endpoint + STRIPE_WEBHOOK_SECRET; Resend RESEND_API_KEY + verified domain; set NEXT_PUBLIC_APP_URL (inconsistent defaults); Supabase Auth → whitelist redirect URLs (/auth/callback, /auth/reset, /auth/invite/*) + enable reset-password email template.
  - **STILL OPEN:** apply **0029** (role RLS + co-member profile names — until then teammate names show "Member · id4", role writes are server-action-enforced only); **email flows (reset/magic-link/invite) UNTESTED** end-to-end (need Supabase redirect config + Resend key) → manual pass after setup; minor: invite email CTA #2563eb→#4F6EF7 + login/AuthLayout DS pass + aria-live on a few errors; listUsers pagination in duplicate-invite guard.
- **D-077** **Rollup-vs-separate is a REPORTING-LAYER preference, not an entry/history/schema concern** (Builder-confirmed 2026-06-02). Multi-tenant case: Church A wants two same-time ministries shown as one occurrence total; Church B wants them shown separately. BOTH store identical per-ministry atoms — the difference is purely how the **dashboard renders** (occurrence-total headline + drill-down vs. ministries side-by-side). Implications: (a) #36 weekly entry is always Service→Ministry→Metrics per-ministry, identical for all churches — the rollup setting never touches entry; (b) History stays at atom/ministry grain for everyone (raw ledger); (c) the preference lives in the dashboard/reporting layer (candidate home: `churches.grid_config` jsonb, precedent = `tracks_*` flags / D-070), computed on read, reversible, no migration. **Scopes OUT of #36 → it's a #62 (dashboard/reporting) concern.** Lever B (model a ministry as its own separate service so occurrences never share an id) remains available for a church that genuinely has no combined-service concept. Open (low-stakes, decide at reporting-build time): church-wide flag vs per-service flag — lean per-service, default "roll up."
- **Gate:** SAGE — direction approved, nothing ships until migration 0028 + FELIX sign-off + IRIS #36 element map + DECISION_REGISTER entry (QUILL). TR-01: `service_templates` is a god node — read graph for blast radius before writing 0028.

## Suggested order
#60 (attendance) → #63 (History data — fast visible win) → #62 (dashboard + reporting metrics) → #61 (tags UI + tree) → #36 (weekly entry) → remaining settings/entry/AI screens → #52 (docs).

## D-098 — Migration 0029 (Settings role-aware RLS) APPLIED to production (2026-06-03)
Applied to `iwbrzdiubrvogiamoqvx` via the BOT gate with explicit per-action user authorization. Adds `get_user_role()` + `is_church_manager()` (SECURITY DEFINER, pinned search_path, mirrors `get_user_church_ids`) and replaces the church-isolation-only ALL policies on `church_memberships`, `church_locations`, `service_template_tags`, `church_invites` with SELECT (isolation) + role-aware INSERT/UPDATE/DELETE (owner/admin = manager writes); plus self-only `memberships_update_self_default` (non-managers may change ONLY their own `default_location_id`); plus `profiles_comember_read` on `user_profiles` (co-member name reads). Touched NO data rows.

**Gate evidence (FELIX → empirical test → SAGE):**
- FELIX verdict PASS-WITH-NOTES. Pre-apply confirmed all 5 live policy names matched the DROP statements exactly (no orphaned permissive policy left ORed alongside the new ones — the key risk), and that `get_user_church_ids` + `enforce_last_owner` exist and RLS is on for all 5 tables.
- FELIX critical Finding #1 (privilege escalation): self-update WITH CHECK `role = get_user_role(church_id)` — if `get_user_role()` saw the NEW role mid-UPDATE it would be a tautology letting a viewer self-promote. **Disproven empirically:** impersonated a seeded viewer (role=authenticated + JWT claims) in a self-rolling-back DO block — NEG_SELF_PROMOTE=BLOCKED[42501], NEG_SELF_DEACTIVATE=BLOCKED[42501], NEG_LOC_INSERT(manager-only)=BLOCKED[42501], POS_DEFAULT_LOC=SUCCEEDED. get_user_role reads the OLD snapshot (Halloween-problem protection) → escalation blocked while the legitimate default-campus write passes. Test left 0 leftover rows (verified).
- SAGE: RATIFIED.

**Residual follow-ups (non-blocking, from FELIX notes):**
- Header says "Idempotent" but new policy names lack DROP-guards → first-apply-only (fine; ledger prevents re-run). If re-run manually, add `DROP POLICY IF EXISTS "<newname>"` before each CREATE.
- Verify an FK/constraint scopes `church_memberships.default_location_id` to a same-church `church_locations` row (a viewer can set it to any UUID otherwise — low-severity cross-church integrity, not privilege).
- `memberships_update_self_default` pins role/is_active/church_id/user_id but not "only default_location_id changed" — confirm no other sensitive self-editable columns on `church_memberships`.
- Effect: teammate names now resolve on the Team screen (was "Member · id4"); member/invite/role/location writes are now DB-enforced, not just server-action-enforced.

## D-099 — Notification channel policy + EMAIL_POLICY.md registry (2026-06-04)
Created `EMAIL_POLICY.md` (repo root) as the canonical source of truth for all notifications: the channel decision rule (in-app pop-up vs Resend email vs Stripe-native vs Supabase-auth), a registry table of every trigger→channel→template→recipient→dedup→code location, and decisions. Channel rule: app-sent email is ONLY for lifecycle nudges to someone NOT currently in the app (trial ending, payment failed, invite, reset); anything in-session is a pop-up. Key decisions: **`aiSetupExhausted` is IN-APP, not email** — the user is mid-import when the trial $1.00 setup budget (covers import_stage_a+b) hits zero and the import 402s; show a pop-up + upgrade link, remove the dead Resend template. `paymentFailed` Resend template is dead (Stripe-native owns it). Two invite mechanisms (Resend custom-token in settings/team vs Supabase inviteUserByEmail in onboarding/invite) need reconciliation to one canonical path (D-096). Audit found only 2 live Resend send sites (trial cron, team invite) + Supabase-auth (magic-link/reset/onboarding-invite) + Stripe-native. Grep audit commands in EMAIL_POLICY.md §5.
